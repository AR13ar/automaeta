alert("JavaScript is running!");
const canvas = document.getElementById('gameCanvas');
const ctx = canvas.getContext('2d');

// Check if the canvas and context are working
ctx.fillStyle = 'blue';
ctx.fillRect(50, 50, 100, 100); // Draws a blue square

